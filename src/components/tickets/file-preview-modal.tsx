/**
 * Re-export desde el componente global en ui/
 * Mantiene compatibilidad con imports existentes en tickets/
 */
export { FilePreviewModal, type PreviewFile } from '@/components/ui/file-preview-modal'
export type { PreviewFile as FilePreviewModalFile } from '@/components/ui/file-preview-modal'
