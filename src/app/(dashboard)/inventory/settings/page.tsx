/**
 * Inventory Settings (gestores) — /inventory/settings
 * Misma UI que admin; fuera de /admin para el proxy de roles.
 */
export { InventorySettingsPage as default } from '@/components/settings/inventory/inventory-settings-page'
