/**
 * Re-exportado por compatibilidad: el helper de errores de Prisma vive en
 * `src/lib/db/prisma-errors.ts` (sin dependencia del módulo de Accesos,
 * reutilizado también por Noticias).
 */
export { isPrismaUniqueViolation } from '@/lib/db/prisma-errors'
